---
---
<div id="view-login" class="login-container">
  <div class="login-logo">🎾</div>
  <h1 class="login-title">網球聯盟會員登入</h1>
  <p class="login-subtitle">請輸入您的手機號碼以驗證會員身份</p>
  
  <div class="card login-card">
    <form id="login-form" onsubmit="event.preventDefault();" novalidate>
      <div id="login-form-error" class="form-error-banner hidden"></div>
      <div class="form-group">
        <label for="login-phone" class="form-label">手機號碼</label>
        <input 
          type="tel" 
          id="login-phone" 
          class="input-control" 
          placeholder="例如：0932028517" 
          pattern="^09[0-9]{8}$" 
          inputmode="numeric" 
          required 
        />
        <div style="font-size: 11px; color: var(--color-text-muted); margin-top: 6px; text-align: left;">
          格式限制：09 開頭共 10 碼數字
        </div>
      </div>
      
      <button type="submit" id="btn-login-submit" class="btn btn-primary">
        <span>驗證並登入</span>
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M15 3h4a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2h-4"/><polyline points="10 17 15 12 10 7"/><line x1="15" y1="12" x2="3" y2="12"/></svg>
      </button>

      <!-- Registration Link for Non-Members -->
      <div style="margin-top: 20px; font-size: 14px; color: var(--color-text-muted);">
        還不是網球聯盟會員？
        <a href="#" id="link-to-register" style="color: var(--color-primary); font-weight: 700; text-decoration: none; margin-left: 4px;">立即註冊會員</a>
      </div>
    </form>
  </div>
</div>
